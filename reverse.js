// function arrayReverse(array) {
//     return array.reverse()
// }


function arrayReverse(array) {
    let reverseNum = []; 

    for(let i = 0; i < array.length; i ++){
        reverseNum.push(array[i])
    }
    return reverseNum
}