const a = 5;
const b = 12;
const c = 9;


function lcm(a, b, c) {
    // დავატრიალოთ ფორ ლუპი და 1000 დან მოვძებნოთ ის  რიცხვი რომელიც გადაცემული სამი რიცხვის ჯერადია. 
   for (let i = 1; i <= 1000; i++) {
      // თუ i უნაშთოდ იყოფა ყველა რიცხვზე დააბრუნე ეს რიცხვი 
      if (i % a === 0 && i % b === 0 && i % c === 0) {
        //console.log(i)
        return i;
    }
    }
} 
 
console.log(lcm(a, b, c));
 